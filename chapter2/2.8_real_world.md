# 2.8 现实世界 (Real world)

大多数操作系统都采用了进程（process）的概念，并且大多数操作系统中的进程在外观和行为上都与 xv6 的进程非常相似。然而，现代操作系统支持在一个进程内包含多个线程（threads），以允许单个进程充分利用多个 CPU 的性能。在一个进程中支持多线程，涉及到大量 xv6 所不具备的底层机制，这通常还包括接口的更改（例如 Linux 的 `clone`，它是 `fork` 的一个变体），以便精确控制多个线程之间究竟共享进程的哪些属性。s